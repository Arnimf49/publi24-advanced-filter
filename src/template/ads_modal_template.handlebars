<style>
    .modal_top_border {
        display: block;
        height: 2px;
        width: 100%;
        position: sticky;
        background: #17b;
        top: 83px;
        z-index: 10;
        margin: auto;
        margin-top: -2px
    }
    @media screen and (max-width: 640px) {
        .modal_top_border {
            top: 75px;
        }
    }
    .ww-modal-header {
        position: sticky;
        z-index: 1;
        top: 0;
        width: 100%;
        max-width: 1000px;
        margin: auto;
        padding: 15px 10px;
        background: #4b4b4b7a;
        backdrop-filter: blur(10px);
        display: table;
    }
    @media screen and (min-width: 1000px) {
        .ww-modal-header {
            padding-left: 0px;
            padding-right: 20px
        }
        .ww-modal-header h2 {
            border-top-left-radius: 0 !important;
            border-bottom-left-radius: 0 !important;
            padding-left: 20px !important;
            padding-right: 20px !important;
        }
    }
</style>

<div style="position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 10000;
            overflow-y: scroll;
" data-wwid="ads-modal">
    <div style="
        pointer-events: none;
        position: sticky;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        background: rgba(0,0,0,0.85);
        backdrop-filter: blur(10px);
        margin-bottom: -100vh;
    "></div>

    <div class="ww-modal-header">
        <h2 style=" font-weight: bold;
                color: #ffffff;
                float: left;
                text-shadow: 2px 2px 2px rgba(0, 0, 0, .2);
                background: #17b;
                padding: 5px 15px;
                border-radius: 10px;
                margin-top: {{#if IS_MOBILE_VIEW}}-1px{{else}}-5px{{/if}};">
            Anunțuri
        </h2>
        <button type="button" class="mainbg radius" style="float: right; border-radius: 1000px; padding: 11px; line-height: 0; background: rgba(255,255,255,0.6)" data-wwid="close">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
                <line x1="4" y1="4" x2="20" y2="20" stroke="black" stroke-width="2"/>
                <line x1="20" y1="4" x2="4" y2="20" stroke="black" stroke-width="2"/>
            </svg>
        </button>
        <button type="button" class="radius" style="color: black;float: right;line-height: 0;background: #c59b2f;
                margin-right: {{#if IS_MOBILE_VIEW}}13px{{else}}25px{{/if}};padding: 13px 6px;" data-wwid="hide-all">
            <b>ascunde toate</b>
        </button>
    </div>

    <div class="rmd-container-search-results" data-wwid="container" style="
        border: 2px solid #17b;
        width: 100%;
        max-width: 1000px;
        padding: 0 !important;
        background: #ececec;
        border-radius: 0 0 10px 10px;
        margin: auto;
        margin-bottom: 150px;
        position: relative;
        {{#if IS_MOBILE_VIEW}}margin-top:105px;{{/if}}
    " onclick="event.stopPropagation()">
        <div class="modal_top_border"></div>
        <div style="padding: {{#if IS_MOBILE_VIEW}}10px{{else}}20px{{/if}} ;">
            <input type="text" style="font-weight: bold; font-size: 1.2rem" data-wwid="phone-input" placeholder="Număr telefon" {{#if phone}}value="{{phone}}" disabled{{/if}}/>
            <p style="color: rgb(55 55 55); font-size: .95rem; padding-bottom: 10px; padding-top: 10px; display: table; width: 100%;">
                <strong style="float: left" data-wwid="results-count">Rezultate: <span data-wwid="count">{{count}}</span></strong>
                {{#if removed}}
                    <span style="float: left; padding-left: 5px; font-style: italic; font-size: .85rem; opacity: 0.8; color: #8b5454">
                        ({{removed}} nu sa mai încărcat)
                    </span>
                {{/if}}
                <span style="float: right; font-style: italic; font-size: .85rem; opacity: 0.8">
                    Pot sa fie mai multe care încă nu au fost analizate.
                </span>
            </p>

            <div data-wwid="content">
                {{{content}}}
            </div>
        </div>
    </div>
</div>
